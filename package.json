{
  "name": "chesschamp",
  "private": true,
  "version": "0.1.0",
  "description": "A personal AI chess coach — Stockfish is the brain, an LLM is the voice.",
  "type": "module",
  "scripts": {
    "lint": "eslint static/*.js",
    "format": "prettier --write \"static/**/*.{js,css,html}\" \"*.md\"",
    "format:check": "prettier --check \"static/**/*.{js,css,html}\" \"*.md\""
  },
  "devDependencies": {
    "@eslint/js": "^9.19.0",
    "eslint": "^9.19.0",
    "globals": "^15.14.0",
    "prettier": "^3.4.2"
  }
}
